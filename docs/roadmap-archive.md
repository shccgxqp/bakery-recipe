# 路線圖封存(2026-07-13 前完整版本存檔)

> `docs/roadmap.md` 在 2026-07-13 做了精簡:已完成項目的決策過程、實作進度、
> 拍板討論全部原文搬來這裡保存;`roadmap.md` 只留一行摘要+版本號+還沒做的部分。
> 這份文件是唯讀歷史記錄,不再更新——新的討論寫回 `roadmap.md`。

---

# 產品路線圖(需求盤點中,尚未實作)

> 建立:2026-07-09。狀態標記:🔴 需求未定 / 🟡 需求大致清楚 / 🟢 可直接動工。
> 每項列出「待拍板的問題」——這些問題有答案,該項才算需求確定。

## 總體目標(第 4 項的願景,統領全部)

打造「**分享烘焙食譜 + 個人工作室營業用 + 營養/過敏原標示**」的網站:
使用者能建立自己的食譜、填寫營養標示、標註過敏原;工作室能算成本毛利。
從「個人工具」轉型為「多使用者平台」。

## 建議實作順序與相依關係

```
小而快 ✅ 全數完成(v3.2.0~v3.4.0):
  7 版本更新頁 · 8 標示下載(完整標籤輸出留待後續) · 10 漲價模擬 ·
  11 合併採購清單 · 14 PWA · 15 資料匯出 · 12 單位換算 · 13 過敏原篩選

Tier 0(2026-07-10 訂,目前最高優先——「功能不好也能靠好 UI 吸引人」):
  16 首頁(Landing Page)+ 17 UI 專業化批次(toast/loading骨架屏/轉場/焦點管理/
  共用元件),兩項要一起做,風格才會一致

Tier 0.5 ✅ 全數完成(v3.7.0):
  18 回收桶復原介面 · 19 複製食譜 · 20 採購清單/模具規格列印

Tier 1 ✅ 完成(v3.8.0):6 網址路由(Hash 路由,今天就上線,以後換乾淨網址無痛)

Tier 2 進行中(v3.9.0~v3.10.0):2 帳號系統——Google 登入 + 信箱密碼註冊/登入
已接通,`users` collection 已建。22 自製確認對話框(取代 confirm())可以跟
帳號系統其他工作穿插做,不特別排序。

基礎建設與單人版功能(還沒動工):
  5 食譜編輯 UI 重造 ⇄ 9 縮放器+模具庫(互相聯動)──┐
  1 拍照快照 OCR ─────────────────────────────┤
  21 登入/註冊專屬頁面 ───────────────────────┘
                                                          ▼
                                4 平台化大改造(公開投稿/警告系統)─→ 3 付費功能
```

---

## 2. 帳號系統:Google 快速登入(完整拍板/實作過程)

Google OAuth 登入;使用者可投稿個人食譜,選擇公開或私人。
**這是整個平台化的分水嶺**,動到資料架構(所有資料加 `ownerId`)、
API 權限模型(從單一密碼變成 per-user 授權)、UI(個人頁)。

- **技術方向定案(2026-07-10 二次修正):手刻 OAuth,不裝 `@auth/core`**。
  原訂 Auth.js,但發現本站前端(GitHub Pages)跟 API(Vercel)是**兩個不同網域**,
  cookie-based session 會被當第三方 cookie(Safari/Firefox 預設擋、Chrome 也在收緊),
  改成 **token 帶在網址、前端存 localStorage、之後 API 呼叫帶
  `Authorization: Bearer <token>`** 這種不依賴 cookie 的做法。既然不用 Auth.js
  的 cookie session 機制,`@auth/core` 的價值所剩不多,改手刻:
  `/api/auth/google/start`(組 Google 授權網址導過去)→
  `/api/auth/google/callback`(收 `code`→跟 Google 換 id_token→驗證→自己簽一組
  JWT,`AUTH_SECRET` 簽章)→導回前端網址帶 `#token=...`,前端讀取存
  localStorage、清網址。跟現有密碼驗證(SHA-256 比對)手刻風格一致,不加依賴。
  現有的單一編輯密碼制在帳號系統上線後轉為「站長權限」,與 per-user token 雙軌並存。
- **schema 已預留**:公開參考資料與私人營運資料欄位已分離(見 db-schema.md),
  屆時私人欄位拆出去加 `ownerId` 即可。
- **拍板結果**:
  - 只開 Google 登入?→ **是**。
  - 訪客(未登入)能看什麼?→ **全部公開資料**(食譜/材料/模具),跟現況一致,
    編輯/上傳才要登入。
  - **資料歸屬政策**:材料、模具**永遠是公開資料,沒有私人切換**(呼應「公開
    材料資料庫」是本站主力價值);**食譜可以自己選公開或私人**。你現有的
    20 道食譜、59 種材料、208 筆模具,材料/模具照舊全公開,食譜歸到你的
    站長帳號名下。
  - 你自己的 20 道食譜屆時預設公開?→ 材料/模具本來就公開;食譜的預設值留到
    第二階段實作時再定(傾向全部先設公開)。
  - 私人食譜存量限制?→ **待拍板,不擋第一階段**(先不設硬限制,正式開放
    註冊前再評估用量抓門檻)。
- **實作進度**:
  1. ✅ **v3.9.0**:接通 Google 登入,不動資料模型。`api/auth/google/start.js` +
     `callback.js`。Google Cloud OAuth 用戶端已建立(專案 `bakejojo`),
     Client ID/Secret 已設進 Vercel production 環境變數。現有 20 道食譜已用
     `scripts/backfill-recipe-owner.mjs` 標上 `recipes.ownerId = "shccgxqp@gmail.com"`,
     先用 email 當識別值(之後要不要換成 Google `sub` 待評估,換值成本低)。
  2. ✅ **v3.10.0**:信箱+密碼註冊/登入補上,`users` collection 正式建立
     (見 db-schema.md)——跟原本規劃的「phase 2 才建 users 表」提前了,因為
     密碼雜湊需要地方存,而且 Google 帳號跟信箱密碼帳號用同一個 email 該是
     同一人,兩件事都需要真正的使用者表。`emailVerified` 欄位留著但不強制
     (驗證信需要驗證過的網域,`bakejojo.com` 還沒買)。側欄陽春信箱密碼測試
     表單,樣式等大改版。
  3. ✅ **v3.12.0(縮小範圍版)**:食譜公開/私人切換上線——但**只有你(站長密碼)
     能切換**,不是真的 per-user 權限。`GET /api/data` 帶對站長密碼
     (header `X-Edit-Password`)才回私人食譜,`RecipeDialog` 加切換開關,
     側欄/詳細頁標 🔒。`ownerId` 欄位還在,但這次沒拿它做權限判斷。
  4. 一般登入使用者建立/編輯自己的食譜、`api/save.js` 逐筆檢查 `ownerId` 擁有權、
     API 權限模型真正雙軌化(單一站長密碼 → per-user 授權)、個人頁、私人食譜
     存量提示——後續在 v3.13.0/v4.0.0 完成,見下。
- **2026-07-10 拍板:phase 4 權限模型設計**——一般使用者開放建立/編輯食譜、
  材料、模具,權限規則:

  | | 食譜 | 材料/模具 |
  |---|---|---|
  | 建立者本人 | 建立/改/刪自己的 | 建立/改/刪自己建的 |
  | 其他一般使用者 | 不能碰別人的 | 不能碰別人建的 |
  | 站長 | **不能改內容**,僅能管理使用者角色;違法/侵權等極端情況保留最後
    人工審核**下架/刪除**權(不能改內容本身,只能整筆下架) | **能改/能刪**
    (資料錯誤要能修正,材料/模具是公開資料庫不是個人創作,修正不算侵犯所有權;
    改動留 `lastEditedBy`/`lastEditedAt` 讓建立者事後看得到,不是無聲覆蓋) |

  這條規則比原本 item 4 草案的「站長是最終仲裁」更嚴——食譜刪除權保留,
  但**編輯**別人食譜的權力完全拿掉,是本次新拍板的收斂。
  - 材料/模具開放一般使用者建立後,連帶需要:`ingredients`/`molds` 加
    `createdBy`/`createdAt`/`lastEditedBy`/`lastEditedAt` 欄位(呼應 item 4
    「上傳者標示」草案,這次提前拍板到 phase 4 一起做);查重機制沿用 item 4
    草案(待拍板:上傳當下模糊比對提示,或站長事後人工合併)。
  - **材料頁瀏覽精簡化**:`IngredientsView.jsx` 列表只顯示重要欄位(名稱/廠牌/
    分類/單價等現有核心欄位),`createdBy`/`createdAt`/`lastEditedBy` 這類「誰
    建的、什麼時候」的溯源資訊移到詳細/編輯視圖才顯示,不擠進列表(現有列表
    欄位已經很多)。
  - **角色分級只先定欄位,不細分實作**:`users.role` 目前只用 `user`/`admin`
    兩種(呼應站長=`admin`),`role` 欄位型別設計上預留 `trusted`(信任貢獻者)
    之類的中間值空間,但實際權限差異(例如信任貢獻者的材料上傳免查重人工複核)
    等真的有多人用時再細定,現在不用想清楚。
  - ✅ **v3.13.0(slice 1:接通寫入權限,2026-07-10)**:上面規則的**核心管線**
    已上線——`api/_lib/auth.js` 統一判斷站長密碼/使用者 token 身份,`api/save.js`
    逐筆檢查擁有權(食譜本人專屬,站長不例外;材料/模具本人或站長皆可),
    `ingredients`/`molds` 新增 `createdBy`/`lastEditedBy`/`lastEditedAt` 欄位。
    一般登入使用者現在真的能建立/編輯/刪除自己的食譜、材料、模具了。用
    throwaway 測試帳號對正式 API 驗證 18 項權限情境(跨使用者竄改擋下、站長
    對食譜無例外、站長對材料例外成立、蓋章欄位正確)全過。
  - **2026-07-10 拍板:slice 2(UI 層)併入 item 4 平台化大改造,不單獨排時程**——
    材料頁列表精簡+詳細頁補溯源資訊、逐項按鈕依權限隱藏(這次按鈕先照樣顯示,
    靠 API 擋+toast)、查重機制、站長角色管理 UI、站長對食譜的極端下架例外,
    這些都是「使用者會直接碰到的 UI/體驗」,跟 item 4 本來就要做的食譜編輯 UI
    重造、材料/模具開放上傳、查重、審核機制是同一批工作,分開做同一批畫面要
    改兩次,見 item 4 段落。
  - ✅ **v4.0.0(2026-07-11,item 4 平台化大改造一次上線)**:slice 2 全部
    隨大改造完成——見 item 4 進度與 `docs/design-guide.md` Part 3(1~11 全 ✅)。
    **同批架構收斂:站長密碼機制整個退場**(`EDIT_PASSWORD_SHA256`/
    `api/verify.js`/`LoginDialog` 已刪),身份完全靠登入 token,站長 =
    `users.role:"owner"`(`scripts/promote-owner.mjs` 已標記你的帳號);
    私人食譜可見範圍收斂成任何身份只看得到自己的。站長對食譜的極端下架
    例外**仍未做**(canWriteRecipe 沒有 owner 分支),留給審核機制一起設計。
- item 5(食譜編輯 UI 重造)與食譜照片欄位,依你的要求併入 item 4
  (平台化大改造)一起想,不併進帳號系統這條線。

## 4. 網頁平台化大改造(完整拍板/實作過程)

✅ **v4.0.0(2026-07-11)頁面與權限層全部上線**:實作進度見
`docs/design-guide.md` Part 3(1~11 全 ✅)——登入/註冊頁、材料/模具/食譜
詳細+編輯整頁化(Dialog 全退役)、食譜分頁籤+共用 Tabs、個人頁 `/me`、
站長使用者管理 `/admin/users`(含停用帳號即時生效)、對外標示卡 `/label/:id`
(含法規同意機制,見 compliance.md 第五節)、查重、站長密碼退場。

原始需求:開放使用者自填營養標示,搭配**防亂標警告系統**:
資料被通報錯誤達一定次數 → 處罰(標示降信任度/隱藏/禁言)。

- **相依**:2(要有帳號才能通報與處罰)。
- **信任機制草案**:每筆營養資料保留「來源」(包裝抄錄/官方資料庫/使用者自填)+
  通報按鈕;通報數過門檻自動下架待審。站長(你)是最終仲裁——**但範圍限縮見
  item 2 的 2026-07-10 權限模型拍板**:食譜只能下架不能改內容,材料/模具
  站長可直接修正。
- **待拍板**:通報幾次觸發?處罰階梯?自填資料要不要先審後上?
  官方(你維護的)資料與使用者資料視覺上如何區分?
- **2026-07-09 補充**:開放範圍不只食譜的營養標示,**食譜/材料/模具三種都要能讓使用者自己上傳**
  (模具庫這次從三能+chefmade型錄建起來,將來使用者應該也能新增自己的模具,不用等你手動建檔)。
  兩個新需求一併考慮:
  - **查重**:使用者上傳前(或上傳後)要偵測跟現有資料(含你自己建的、其他使用者建的)
    是否重複——材料/模具最容易撞名或撞規格(例如兩個人都上傳「三能 6吋活動圓模」)。
    做法待拍板:上傳當下用名稱模糊比對跳警示「已有相似資料,要繼續新增還是用現有的?」,
    還是交給站長事後人工合併?
  - **上傳者標示**:每筆資料要留 `createdBy`(`ownerId`,呼應第 2 項的帳號系統),
    UI 顯示「由 XXX 上傳/建立」——牽動信任機制草案(見上一段)裡「使用者自填」來源
    要能追溯到具體帳號,不是只標「使用者自填」四個字。
- **2026-07-10 補充**:依你的要求,**第 5 項(食譜編輯 UI 重造)跟食譜照片欄位
  (第 1 項底下提過的補充)併進這裡一起想**——這三個都算「使用者會直接碰到的
  UI/使用者體驗」,平台化大改版時一次設計比較不會走回頭路。
- **2026-07-10 補充(item 2 slice 2 併入)**:phase 4 權限模型的 API 管線已經
  上線(v3.13.0,見 item 2),剩下的 UI 層一併排進這裡:
  - **材料頁列表精簡+詳細頁補溯源資訊**:`IngredientsView.jsx` 列表只留核心
    欄位,`createdBy`/`createdAt`/`lastEditedBy` 這類溯源資訊移到詳細/編輯
    視圖才顯示——跟上面「上傳者標示」需求同一件事。✅ v4.0.0 完成。
  - **逐項按鈕依權限隱藏**:目前 v3.13.0 是「按鈕照樣顯示,無權限的寫入由
    API 擋+toast」,大改版時應該做成看得到才顯示(`canEdit(doc)` 判斷)。
    ✅ v4.0.0 完成(`src/lib/permissions.js`)。
  - **站長角色管理 UI**:`users.role` 欄位已存在,還沒有介面能改——大改版
    的帳號/使用者管理頁一起做。✅ v4.0.0 完成(`/admin/users`)。
  - **站長對食譜的極端下架例外**:目前完全沒有(`canWriteRecipe` 沒有 owner
    分支),要不要做、怎麼做(整筆下架但不能改內容?)留到這裡跟審核機制
    一起設計。**仍未做**。

### 2026-07-10 拍板:頁面清單 + 使用者動向 + 視覺風格指南

**完整內容見 `docs/design-guide.md`**(頁面清單、使用者動向、字體/配色/圓角/
響應式策略、AI slop 避雷清單),不在這裡重複維護。摘要:食譜/材料/模具的
新增編輯改成獨立整頁(不再是 Dialog);材料/模具新增獨立詳細頁(有自己網址,
顯示 `createdBy`/`lastEditedBy` 溯源);新增個人頁 `/me`、站長使用者管理頁
`/admin/users`、對外營養標示卡分享頁 `/label/:id`(不含成本/毛利,取代下載
PNG 貼標籤);食譜詳細頁拆分頁籤(基本資料/營養標示/步驟/成本毛利);視覺
風格延伸現有 `paper`/`ink`/`yolk` 系統,不套用市場研究出的「暖色食品網站
公式」(那套已經是 2026 年 AI 建站工具的第二波預設審美)。

## 5. ✅ 新增食譜/編輯頁 UI 重造(v4.0.0 隨 item 4 完成)

`RecipeEditView` 整頁編輯器:單頁分段落+錨點導覽、配方搜尋式下拉
(`IngredientPicker`)+內嵌快速新增材料、克為唯一單位。

- **方向候選**:對話框改整頁編輯器;分步驟(基本資料→配方→步驟→標示);
  配方輸入支援連續快速輸入(打名稱+Enter 直接下一項)、常用材料快選;
  手機輸入體驗優先(工作室現場常用手機)。

## 6. ✅ 網址與 API 優化:每頁專屬網址(v3.8.0,2026-07-10 完成)

需求:食譜有專屬網址(分享連結直達)。

- **定案:Hash 路由**(`react-router-dom` 的 `HashRouter`,網址長 `#/r/:id/:名稱`)。
  GitHub Pages 是純靜態站,# 後面的路徑不會送到伺服器,不用 404 轉址 hack、
  不用把主網址搬去 Vercel——今天直接上線,零部署風險。
  代價:網址多一個 `#`,SEO 掛零(hash 路由連 GET 都不會帶路徑給搜尋引擎)。
  以後要換乾淨網址(`/r/:id/:名稱`,SEO 生效),把 `HashRouter` 換成
  `BrowserRouter`(+ `basename`)即可,`react-router` 的路由定義完全不用改;
  屆時再一併評估搬去 Vercel 做預渲染(呼應「公開材料資料庫要被烘焙業者搜到」的需求)。
- **網址格式定案**:`/r/<id>/<名稱>`——id 是定位依據,名稱只是分享連結好看(裝飾用,
  讀取時完全不看),改名字不會斷連結。其他頁面:`/ings`(材料)、`/molds`(模具庫)、
  `/trash`(回收桶)、`/changelog`(更新紀錄)。
- `App.jsx` 的 `view`/`selId` 從獨立 state 改成唯一信任網址(`useLocation`/`useNavigate`
  推導),所有切頁都變成 `navigate(...)`,不再有雙重狀態源。
- 食譜 Detail 頁加「🔗 複製連結」,呼應「分享連結直達」的原始需求。

## 7. ✅ 版本更新專屬頁面(v3.2.0 完成)

面向使用者的「更新紀錄」頁:只寫「優化了什麼、追加了什麼功能」,不寫技術細節。

- **做法**:資料源用 `src/data/releases.js`,側欄版本號點下去直達此頁。

---

## 8. 營養標示快捷下載 + 完整標籤輸出(基本版拍板/實作過程)

食譜頁一鍵把營養標示表輸出成 PNG 圖片下載。**免責聲明烙印在圖片下緣**
(隨圖轉傳,不可分離):「本標示由公開資料庫試算,僅供參考;販售用正式標示
依法應以實際檢驗數據為準,使用前請自行核對。」

- 順勢補**合規缺口**:台灣營養標示還有「每日參考值百分比」欄
  (熱量 2000 大卡、蛋白質 60g、脂肪 60g、飽和脂肪 18g、碳水 300g、鈉 2000mg;
  反式脂肪與糖標「＊」),下載版要含這欄才是完整格式。
- 前端 canvas 產圖即可,無後端工作。
- ✅ 3.2.0/3.2.1 已完成:標示 PNG 下載+每日參考值%+浮水印+下載前同意。

## 9. ✅ 配方縮放器 + 模具資料庫(v3.3.0,2026-07-09 完成)

兩種模式:
- **按份數縮放**:全食譜通用(餅乾/馬卡龍/手指餅乾/醬料),永遠可用。
- **按模具換算**:蛋糕/磅蛋糕/塔類,「6吋配方 → 8吋模」以容積比換算;
  **食譜必須綁定模具才開放此模式**(輸入嚴謹化的動力)。

**模具刻度已定案:幾何尺寸為準、品牌當備註(粗刻度)**——
倍率由容積(直徑²×高)決定,各品牌同尺寸差異(5~10%)小於烘焙本身容錯,
品牌精算是假精度;「拉高版」= 同直徑不同高度 = 幾何制下自然是另一筆資料。
新 collection `molds`:形狀(圓/方/長條/咕咕霍夫/塔圈…)、尺寸、高、名稱備註
(品牌寫在名稱裡當識別,計算只看尺寸)。食譜加選填 `moldId`,
編輯器用模具下拉選單取代備註裡手寫的「方形模21.5cm」。
塔皮類(皮按面積、餡按容積)第一版先全配方等比+提醒。
換算結果一律附註:烤溫烤時需人工調整。

## 10. ✅ 材料漲價模擬(v3.2.0 完成)

「奶油 +10%,哪些食譜毛利掉最多?」——選材料、調漲幅,即時列出受影響食譜的
成本/毛利變化排行。純前端計算,`PriceSim.jsx`。

## 11. ✅ 多食譜合併採購清單(v3.2.0 完成)

勾選多道食譜(本週生產計畫)→ 合併同材料用量 → 一張總採購清單+預估花費,
`ShoppingDialog.jsx`。

## 12. ✅ 單位輸入換算(v3.4.0,2026-07-09 完成,後於 v4.2.0 移除)

材料加選填 `unitName`/`unitGrams`(顆=55g、大匙=14g…),配方輸入時可用
「3 顆」自動轉公克;採購清單同步顯示「蛋 8 顆(440g)」。
**2026-07-11 更新:v4.2.0 已移除**——查了 59 筆材料沒有任何一筆設定過,
對食譜配方沒有實質意義,詳見 CHANGELOG.md 4.2.0。

## 13. ✅ 過敏原篩選(免登入,v3.4.0,2026-07-09 完成)

食譜列表加「排除過敏原」多選(排除花生、堅果…),依材料標註即時過濾。
未來付費功能「過敏品通知」的免費前身,先驗證需求。

## 14. ✅ PWA(v3.2.0 完成)

manifest + service worker,手機當 App 用,工作室現場查配方;離線快取。

## 15. ✅ 資料匯出(v3.2.0 完成)

一鍵下載全部資料(JSON/CSV)。對未來註冊使用者是資料自主權的信任感,
對站長是多一層手動備份。

---

## 16. ✅ 首頁(Landing Page,v3.5.0,2026-07-10 完成)

現況(2026-07-10 前):網站沒有獨立首頁,`App.jsx` 一開站就直接進主畫面
(側欄+食譜清單),瀏覽公開、編輯才要密碼,完全沒有「介紹」層。

- **結構**:Hero(沿用既有 tagline「配方‧成本‧營養,一頁看完」+ 講清楚是給誰用的
  +單一主要 CTA「進入瀏覽」+ 次要「🔑 我是主人,登入編輯」沿用現有登入邏輯)→
  產品截圖區(秀真實畫面,不是插畫)→ 功能亮點卡 3~4 張(成本試算/營養標示合規/
  模具換算/過敏原篩選,各一句話講具體結果)→ 信任/差異化區(這站的獨特賣點是
  **法規合規**,`docs/legal/compliance.md`,別的個人烘焙工具通常沒有)→ 頁尾
  (版本號/更新紀錄連結)。

## 17. ✅ UI 專業化批次(v3.6.0,2026-07-10 完成)

- **Toast 通知取代 `alert()`/`prompt()`**:全站 14 處 `alert()` + 登入用原生
  `prompt()`,跟站內自訂 `Dialog` 風格不搭,是最明顯的業餘感來源。
- **Loading 骨架屏**:開站讀取資料時沒有骨架屏或明顯提示。
- **轉場動畫**:全站目前 0 筆 transition CSS,對話框開關、頁面切換、清單篩選都是
  瞬間切換。
- **對話框焦點管理**:`Dialog.jsx` 有 `role="dialog"` 但沒有 focus trap。
- **共用 Chip picker 元件**:`AllergenPicker`、Sidebar 排除過敏原、模具形狀
  分組 chips 是同一種視覺樣式手刻三次,合併成一個共用元件。
- **格式化邏輯收斂**:`fmt()` 在 `calc.js`,但各處有自己的 `toFixed`/`Math.round`。
- **設計 token 補齊**:延伸 `paper`/`ink`/`yolk`/`warn`/`ok` 這組 token,補
  spacing scale、陰影層級(elevation)、動態效果(motion)token。

## 18. ✅ 回收桶復原介面(v3.7.0,2026-07-10 完成,獨立頁面)

刪除確認文字寫「可從資料庫救回」(軟刪除機制已存在),但實際救回要手動查 git
備份或連 MongoDB,站內完全沒有「回收桶」頁面可以自己按復原。

## 19. ✅ 複製食譜(v3.7.0,2026-07-10 完成)

做口味變體(如「原味→抹茶」)目前只能整份重新輸入。「複製」按鈕產生一份新
`_id`、名稱加「(複製)」的副本,開編輯對話框讓使用者改。

## 20. ✅ 採購清單/模具規格列印(v3.7.0,2026-07-10 完成)

目前只有食譜 Detail 頁有 🖨 列印,`ShoppingDialog`(採購清單)、模具規格
想印出來貼工作台沒有對應功能,沿用 Detail 頁的 `window.print()` 做法即可。

## 21. ✅ 登入/註冊專屬頁面(v4.0.0,隨平台化大改造完成)

現在登入/註冊(Google + 信箱密碼)都只是側欄裡的陽春測試按鈕跟表單,沒有
獨立的頁面。等第 4 項平台化大改造(連同第 5 項、`users` 欄位補完整)
一起設計。

## 22. ✅ 自製確認/警告對話框,取代瀏覽器原生 `confirm()`(v3.11.0,2026-07-10 完成)

第 17 項只換掉了 `alert()`/`prompt()`,**`confirm()` 當時刻意留著沒動**
(`Detail.jsx` 下載營養標示前的合規同意提示、`App.jsx` 的刪除確認)。原生
`confirm()` 樣式醜、不能自訂版面,「需要使用者明確同意某件重要事情」這種場合
(尤其營養標示下載前的法規同意聲明)理論上更需要清楚、不容易誤按的畫面。

- **用途**:下載營養標示前的合規同意聲明、刪除確認、未來任何「需要使用者
  勾選我同意」的場合(如服務條款同意)。
- **做法**:比照 `Dialog.jsx` 的風格做 `ConfirmDialog.jsx`,回傳 Promise
  (`await confirmDialog({ title, body, confirmText })`),取代
  `if (!confirm(...)) return` 這種寫法。
