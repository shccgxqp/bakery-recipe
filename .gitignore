# 環境變數(MongoDB 連線字串等機密)— 絕對不能 commit
.env
.env.*
!.env.example
*.json.key

# 一次性資料匯入腳本,僅本機使用
scripts/import-legacy.mjs
node_modules/
dist/
.vercel
.env*

# Local coordination state shared by AI agents in this working copy.
# The protocol and CLI are tracked; active claims and handoffs are deliberately not.
.ai-team/runtime/
