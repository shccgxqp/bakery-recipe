# 版本紀錄

## 版本規則

版本號 `主.次.修訂`(語意化版本),**唯一來源是 `package.json` 的 `version`**,
網頁左側清單最下方會自動顯示目前版本。

- **主版本**:架構級變動(資料層、部署方式、資料格式不相容的改版)
- **次版本**:新功能(新頁面、新欄位、新的操作方式)
- **修訂**:修 bug、文案、視覺微調

**每次 push 部署前必做**:`package.json` 的 `version` +1,並在下方加一段紀錄(新的在上面)。

---

## 4.5.0 — 2026-07-12

- 導覽分流：公開探索與分享食譜維持獨立閱讀頁；登入者以「我的帳本」進入自己的配方清單、成本與採購工作區。
- 新增 `/ledger` 與 `/ledger/r/:id/:name`；工作側欄只顯示本人公開／私人食譜，並可依可見性篩選。材料、模具、個人資料與公開詳情頁不再顯示食譜側欄。
- 公開食譜頁不提供成本、編輯、刪除或複製管理動作，分享連結固定指向可公開閱讀的食譜網址。

## 4.4.1 — 2026-07-12

- 食譜頁桌面版改兩欄:配方與營養標示/過敏原併排(互相對照不用捲動),
  步驟與烘烤接在配方正下方,成本與毛利全寬底部。用 grid 定位不動 DOM
  順序,手機分頁籤與列印單欄輸出都不受影響。

## 4.4.0 — 2026-07-12

- 食譜頁右上角按鈕整理:複製連結/複製購買清單/傳到 LINE/列印食譜卡/
  複製食譜(登入才顯示)五項收進「📤 分享」下拉選單,右上角最多剩四顆
  (換算/分享/編輯/刪除,後兩顆只有作者本人看得到);複製回饋改 toast。
- 食譜詳細頁分頁籤改成**只在手機出現**:桌面空間夠,配方/營養標示與過敏原/
  步驟與烘烤/成本與毛利四個區塊一次全部顯示(各帶帳本規線區段標題),
  手機維持分頁籤切換;列印整頁輸出不受影響。

## 4.3.0 — 2026-07-12

導覽結構重整(參考 Cookpad 國際版/King Arthur Baking/Mealie 的慣例分工:
上方 bar 管全站內容區塊、側欄只管當前工作區清單)。

- **新增上方導覽列 `TopBar`**:品牌(→探索)/探索食譜/材料庫/模具庫
  導覽連結+右側「＋新增食譜」與個人下拉選單(個人頁/回收桶/備份/
  使用者管理(站長)/更新紀錄/登出)。landing、登入註冊、對外標示卡
  (客人頁)不顯示。
- **新增探索頁 `/explore`**:所有人公開食譜的卡片牆(名稱/分類/大卡每份/
  過敏原/作者暱稱),名稱與烘焙師即時搜尋+分類篩選——「進入瀏覽」與
  登入後的預設首頁(原直接進 /r 工作區)。
- **側欄瘦身**:材料主檔/模具庫切換、備份、回收桶、使用者管理、登入登出
  全部移入 TopBar;側欄只剩搜尋/排序/過敏原篩選/食譜清單/新增/採購清單。
- **email 隱私修正(政策紅線)**:`GET /api/data` 公開回應不再含任何
  email——`ownerId`/`createdBy`/`lastEditedBy` 在伺服器換成 `ownerName`/
  `creatorName`/`editorName`(暱稱)與 `mine`/`editedByMe` 旗標才出門;
  前端權限顯示(permissions.js)與個人頁清單改吃旗標;材料/模具詳細頁
  溯源顯示暱稱,原 email 遮罩邏輯移除。寫入授權不受影響(仍在伺服器
  以資料庫 email 比對)。

## 4.2.0 — 2026-07-11

- **烘烤設定改結構化**:`bakes` 從純文字陣列改成 `{temp, time, note}` 一段一筆,
  編輯頁三欄輸入(溫度/時間/備註)取代自己拼字串;詳細頁顯示同步更新。
  既有 6 道有烘烤資料的食譜已遷移(原味奶油棒、厚燒奶油酥餅、雪球餅乾、
  馬卡龍、柳丁磅蛋糕、香蕉磅蛋糕)。
- **參考食譜連結改一筆一列**:編輯頁改成「標題」「網址」左右兩個輸入格,
  取代原本「標題 | 網址」擠在同一行用 `|` 分隔的寫法(資料本來就是
  `{title, url}` 物件,只是編輯 UI 改善,無需遷移)。
- **配方「層」欄位更名為「所屬部分」**:標籤更直覺,補一行說明「這項材料
  屬於食譜的哪個組成(例:餅乾層、奶油層、內餡)——同名的部分會自動歸在
  同一段顯示;單層食譜留空即可」。
- **移除材料的「單位換算」功能**:`unitName`/`unitGrams` 欄位與相關 UI 全部
  移除——查了 59 筆材料沒有任何一筆設定過,水果/蔬菜這類單顆重量本來就
  不固定,這個換算對食譜配方沒有實質意義。配方的「用量 g」欄位現在旁邊
  常駐顯示「g」單位,不用等打完數字才確認單位。

## 4.1.0 — 2026-07-11

- 食譜編輯頁「作法步驟」改一步一格輸入(取代單一大文字框換行切):每步有
  編號徽章、獨立輸入格、可在任意步後插入(＋)或刪除(×),使用者清楚看到
  第幾步是什麼。資料層本來就是一步一筆的陣列,只改編輯 UI;空格存檔時
  自動略過。

## 4.0.1 — 2026-07-11

- 修 `/api/label`:食譜缺 `items` 欄位時會 500(正常資料都有,防禦性補上)。

## 4.0.0 — 2026-07-11

平台化大改造(roadmap 第 4 項)一次上線,含身份架構級變動,故主版本 +1。
設計依據見 `docs/design-guide.md`(頁面清單/使用者動向/視覺風格/實作順序)。

- **站長密碼制退場(架構變動)**:`EDIT_PASSWORD_SHA256`/`/api/verify`/
  `LoginDialog` 全移除,登入 token 是唯一身份;站長 = `users.role:"owner"`
  (你的 Google 帳號已標記)。私人食譜可見範圍收斂:任何身份都只看得到
  自己的私人食譜,站長沒有例外。
- **對話框退役,新增/編輯全面整頁化**:`RecipeDialog`/`IngredientDialog`/
  `MoldDialog` 刪除,改為 `/r/new`、`/r/:id/edit`、`/ing/new`、`/ing/:id/edit`、
  `/mold/new`、`/mold/:id/edit` 整頁編輯器;食譜編輯頁為單頁分段落+錨點導覽,
  配方改搜尋式下拉(`IngredientPicker`,打字即時候選)+內嵌快速新增材料
  (名稱+分類最小欄位,跟正式新增同一套查重,共用 `src/lib/dedup.js`)。
- **材料/模具詳細頁**:`/ing/:id/:name`、`/mold/:id/:name` 獨立網址,完整資料+
  建立者/修正者溯源(email 遮罩,真名/email 永不公開);材料列表 14 欄精簡為
  8 欄,細節進詳細頁看。
- **食譜詳細頁拆分頁籤**(共用 `Tabs.jsx`):配方/營養標示與過敏原/步驟與烘烤/
  成本與毛利——成本毛利只有本人(與站長)看得到,配方表的成本欄對訪客同步隱藏;
  列印食譜卡不受影響。編輯/刪除按鈕改逐項權限判斷(`src/lib/permissions.js`)。
- **登入/註冊頁**(`/login`、`/register`):取代側欄測試表單;註冊強制勾選
  服務條款(`tosAcceptedAt` 存伺服器);Google 建帳號暱稱一律留空不抄真名,
  首次登入導去個人頁取暱稱。
- **個人頁 `/me`**:暱稱/工作室/個人網頁(`GET/POST /api/auth/me`,存檔回
  重簽 token)、貢獻統計、我的食譜(公開/私人篩選)/材料/模具清單。
- **站長使用者管理 `/admin/users`**:改角色(user/trusted/owner)、停用帳號;
  寫入路徑改 `resolveCallerChecked`(查 DB)→ 停用與角色變更即時生效,
  不受舊 token 30 天效期影響;防呆不能改自己/停用自己。政策邊界:只動
  users 表,完全不碰使用者的食譜/材料內容。
- **對外營養標示卡 `/label/:id/:name`**:給客人/包裝 QR 的公開頁(不用登入、
  不受公開/私人影響),內容物依重量遞減+營養 8 項+過敏原+保存;專屬端點
  `GET /api/label` 後端算好才回,成本/售價/配方克數不出後端。合規三件套
  (見 compliance.md 第五節新增):建立者產生連結前一次性同意存伺服器、
  頁面浮水印+常駐免責、明示「連結不可取代包裝上的法定標示」(第 22 條)。
- 其他:全域 ↑↓ 快捷鍵在編輯頁停用(避免切走頁面丟失未存檔內容);
  厚燒奶油酥餅步驟資料重切為一步一格(5→9 步);圓角 token
  (`--radius-sm/md/lg/pill`)落地;design-guide 新增「帳本規線」簽名系統
  與 AI slop 避雷清單。

## 3.13.0 — 2026-07-10

- 帳號系統 phase 4 slice 1(roadmap 第 2 項):**接通一般登入使用者的真實寫入權限**。
  新增 `api/_lib/auth.js`,`POST /api/save` 現在會逐筆檢查擁有權——食譜只有
  `ownerId` 相符的本人能寫/刪/復原,站長密碼登入也一樣受這條規則約束(不能碰
  別人的食譜內容);材料/模具則是建立者本人或站長皆可(公開資料庫,站長要能
  修正錯誤資料),站長編輯時蓋 `lastEditedBy`/`lastEditedAt`。新文件的
  `ownerId`/`createdBy` 一律伺服器蓋章,不信任 client payload。前端
  `pushData`/`write()` 改成站長密碼或使用者 token(`Authorization: Bearer`)
  皆可寫入,`isEditor` 兩種登入身份都算(按鈕先照樣顯示,無權限的寫入由 API
  擋下+toast 顯示)。這次只做權限管線,材料頁 UI 精簡、查重、審核機制留到
  之後的 slice。

## 3.12.0 — 2026-07-10

- 帳號系統 phase 2(縮小範圍版,roadmap 第 2 項):食譜可以切公開/私人。
  `RecipeDialog` 加「公開/🔒私人」切換;`GET /api/data` 沒帶對站長密碼
  (header `X-Edit-Password`)只回公開食譜,私人食譜訪客看不到也連不到;
  帶對密碼(登入編輯時)回全部。側欄清單、食譜詳細頁對私人食譜都加 🔒 標記。
  順便補上一個既有缺口:登入/登出之前都沒有重新讀取資料,現在都會
  (不然私人食譜要重新整理頁面才會出現/消失)。這次只有你(站長密碼)能切換,
  一般登入使用者建立/管理自己食譜的完整權限模型留到之後範圍確定再做。

## 3.11.0 — 2026-07-10

- 自製確認對話框(roadmap 第 22 項):`ConfirmHost.jsx` + `lib/confirm.js`
  (跟 toast 系統同一套 pub-sub 模式)取代全站最後 4 處原生 `confirm()`——
  刪除食譜/材料/模具的確認,以及營養標示下載前的法規同意聲明(這個特別適合換掉,
  長版合規文字用原生 confirm 排版很差)。

## 3.10.0 — 2026-07-10

- 帳號系統加信箱+密碼註冊/登入(roadmap 第 2 項):新增 `users` collection,
  密碼用 Node 內建 `crypto.scrypt` 雜湊(不裝套件),連續登入失敗 5 次鎖 15 分鐘。
  Google 登入跟信箱密碼登入用同一個 email 判斷是不是同一人(帳號整合),
  `api/auth/google/callback.js` 跟著改成查/建 `users` 文件。這次不寄驗證信
  (SendGrid 免費方案已取消,Resend 免費層需要驗證過的網域才能寄給任意收件者,
  網域還沒買),`emailVerified` 欄位先留著。側欄加陽春的信箱密碼測試表單,
  樣式等大改版一起做。

## 3.9.0 — 2026-07-10

- 帳號系統第一階段(roadmap 第 2 項):Google 登入接通測試。手刻 OAuth
  (`api/auth/google/start.js`/`callback.js`,不裝 `@auth/core`)——前端跟 API
  是不同網域,cookie session 會被第三方 cookie 擋掉,改成 token 帶在網址
  query string、前端存 localStorage。側欄加「🧪 Google 登入測試」按鈕,
  純驗證登入這條路通不通,**不影響任何現有權限/資料**,站長密碼制原封不動並行。

## 3.8.0 — 2026-07-10

- 網址路由(roadmap 第 6 項):`react-router-dom`(Hash 路由,GitHub Pages 純靜態站
  不用 404 轉址 hack、不用換部署平台);食譜、材料、模具、回收桶、更新紀錄都有
  真實網址(`#/r/:id/:名稱`、`#/ings`、`#/molds`、`#/trash`、`#/changelog`),
  可分享/加書籤/上一頁下一頁;食譜頁加「🔗 複製連結」
- 網址是唯一真相來源(`App.jsx` 的 `view`/`selId` 改成從網址讀,不再是獨立 state)

## 3.7.0 — 2026-07-10

- 回收桶(roadmap 第 18 項):新增 `POST /api/deleted`(登入才能讀,已刪除資料不公開)+
  `POST /api/save` 支援 `restores`,獨立頁面 `TrashView.jsx` 列出已刪除的材料/食譜/模具,
  一鍵復原
- 複製食譜(第 19 項):食譜頁「📋 複製食譜」立即存一份副本(自動避開名稱重複),
  存完直接開編輯讓你改
- 列印(第 20 項):採購清單、模具庫加「🖨 列印」;`Dialog.jsx` 補上全域 print 樣式,
  對話框內容列印時不再被浮層/捲動框裁切

## 3.6.0 — 2026-07-10

- UI 專業化批次(roadmap 第 17 項):
  - 共用 `Chip` 元件取代 7 處手刻的切換按鈕(過敏原勾選、排序、單位切換、
    模式切換、食譜勾選)
  - `Dialog.jsx` 補 focus trap(Tab 循環鎖在框內、關閉還焦點給觸發按鈕)+
    開關轉場動畫(尊重 `prefers-reduced-motion`)
  - 新增 toast 通知系統(`toast.js`/`ToastHost.jsx`),取代全站 14 處
    `alert()`;登入改用 `LoginDialog`(取代原生 `prompt()`,跟站內對話框
    同風格,密碼錯誤顯示在對話框內而不是跳原生視窗)
  - 冷啟動(無快取)時顯示骨架屏(`Skeleton.jsx`),取代空白畫面

## 3.5.0 — 2026-07-10

- 新增首頁(`Landing.jsx`):開站先看到「焙啾啾」品牌介紹頁,而不是直接進主畫面
  ——Hero + 真實食譜示範卡(現算成本/毛利/過敏原,非假資料)+ 4 項功能亮點 +
  法規合規信任區(含迷你營養標示示意)+ 網站宗旨。「進入瀏覽」/「登入編輯」
  兩個入口,不放註冊(帳號系統還沒做)
- 網站更名「焙啾啾」(烘焙帳本),README 加上網站宗旨(為什麼做這個)

## 3.4.0 — 2026-07-09

- 材料單位換算(`unitName`/`unitGrams`,如「顆」=55g):食譜配方輸入配方時可直接填數量
  自動轉克數;採購清單同步顯示「蛋 8顆(440g)」
- 食譜列表加「排除過敏原」多選篩選(依材料含有/可能含有的過敏原即時過濾)
- 模具庫顯示單位可切換 cm/mm/吋(純顯示層,資料庫仍存 cm);容積欄位改標示「cc」

## 3.3.0 — 2026-07-09

- 模具資料庫(`molds` collection,幾何制:round/square/rect/tube/tart/other,
  容積由尺寸計算,other 手動填;`src/lib/molds.js`);獨立「模具庫」頁面
- 配方換算 `ScaleDialog`:按份數(全食譜可用)/按模具(食譜綁 `moldId` 才開放,
  倍率=容積比);結果唯讀+複製,固定附烤溫烤時警語
- 食譜編輯表單加「模具」下拉;食譜頁顯示模具 chip、加「⇄ 換算」按鈕
- API `data`/`save` 與每日備份納入 molds

## 3.2.1 — 2026-07-09

- 法律合規強化(《食品安全衛生管理法》列為最高準則,見 docs/legal/):
  - 法規全文副本 + `compliance.md`(條文對照、風險模型、免責標準文字、開發鐵律)
  - 標示圖片加斜向浮水印「僅供參考.不得直接用於商業包裝」
  - 下載前一次性同意(localStorage `bakery-label-terms-v1` 留存同意時間)
  - 頁面與圖片免責文字強化:載明法源(第 28/45 條)、罰鍰額度、SGS/台美送驗引導

## 3.2.0 — 2026-07-09

- 營養標示 PNG 下載(canvas 繪製,免責聲明烙印圖片下緣);
  標示表補「每日參考值百分比」欄與參考值註腳(NUTR 陣列加第 5 欄 DV 值)
- 材料漲價模擬(`PriceSim.jsx`,材料頁內收合面板)
- 多食譜合併採購清單(`ShoppingDialog.jsx`,同材料合併、依材料分類排列)
- 使用者版更新紀錄頁(`ChangelogView.jsx`,資料源 `src/data/releases.js`,
  側欄版本號點擊進入;**bump 版本時 releases.js 也要加一筆**)
- 資料匯出:全庫 JSON 備份 + 材料 CSV(含 BOM,Excel 可開)
- PWA:manifest + service worker(shell 採 stale-while-revalidate,/api/ 不快取)

## 3.1.0 — 2026-07-08

- 材料主檔:即時搜尋(名稱/廠牌/規格/分類),結果依分類分區段顯示
- 材料新增「分類」欄位,採台灣烘焙材料行通用 13 分類,順序可在資料庫 settings 調整
- 儲存改為直接寫入資料庫(儲存中鎖定、失敗留在原地),移除側欄雲端同步箱
- 左側清單底部顯示版本號;新增本版本紀錄檔

## 3.0.0 — 2026-07-08

- 資料層全面遷移:MongoDB Atlas(資料庫)+ Vercel Serverless Functions(API),
  push 即自動部署,不再需要手動維護資料來源
- 材料/食譜改以 UUID `_id` 互相引用,材料改名不再影響食譜
- 材料新增合規欄位:廠牌、規格、過敏原(含有/可能含有)、成分(照包裝)、標示登記日;
  「無營養資料」與「數值為 0」正式區分
- 食譜新增:成品重(營養標示每 100g 改以成品重計)、保存期限、保存條件
- 食譜頁自動顯示過敏原資訊(由材料即時聚合)
- 軟刪除(誤刪可救)、每日自動備份到 repo(`backup/*.json`)

## 2.0.0 — 2026-06

- React 19 + Vite 重寫,分類清單、成本/利潤率排序、鍵盤操作、列印食譜卡
- 台灣營養標示 8 項必要欄位補齊(新增飽和脂肪、反式脂肪、鈉)

## 1.x

- 最初的單頁版(靜態 HTML)
